#include <Arduino.h>

void setup() {
	Serial.begin(115200);
	// Read settings
	// Set up Light
	// Attempt to set up wifi. If that fails, create AP
	// Start web server
	// Set up to receive data
}

void loop() {
	// Listen for incoming data
}